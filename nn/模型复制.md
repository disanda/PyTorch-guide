```py
import torch
import torch.nn as nn
import torch.nn.functional as F

class net(nn.Module):
  def __init__(in=32,out=10):
    super().__init__()
    self.conv1 = nn.Conv2d(32,100,1)
    self.conv2 = nn.Conv2d(100,10,1)
  def forward(self,x):
    y = self.conv1(x)
    y = self.conv2(x)
    y = F.sigmoid(y)
    return y
```
